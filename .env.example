# In Vercel, adding a Postgres integration will usually inject POSTGRES_URL automatically.
# You can also use DATABASE_URL if your provider exposes that instead.
POSTGRES_URL="postgres://username:password@host:5432/database?sslmode=require"
